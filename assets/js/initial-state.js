let sprite = document.getElementById('sprite');
let resetPoints = document.getElementById('timer')

function initialState(){
    Sprite.style.display.innerHTML = none;
    resetPoints.innerHTML = 0;
    gameBox.removeChild(newWall);
}